apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: smarterp-admin-route
  namespace: smarterp
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: ingress
  hostnames:
    - "admin.etsoo.cn"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: smarterp-admin
          port: 8080
