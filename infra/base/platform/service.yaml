apiVersion: v1
kind: Service
metadata:
  name: smarterp-platform
  namespace: smarterp
  labels:
    app: smarterp-platform
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: smarterp-platform
