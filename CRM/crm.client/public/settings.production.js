window.settings = {
  /**
   * Endpoint of the API service
   */
  endpoint: "/api/",

  /**
   * App root url
   */
  homepage: "",

  /**
   * Web url of the cloud
   */
  webUrl: "https://{hostname}/",

  /**
   * Endpoints
   */
  endpoints: {
    platform: {
      endpoint: "https://{hostname}/api/",
      webUrl: "https://{hostname}/"
    }
  }
};
