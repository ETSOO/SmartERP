apiVersion: apps/v1
kind: Deployment
metadata:
  name: smarterp-admin
  namespace: smarterp
  labels:
    app: smarterp-admin
spec:
  replicas: {{ .Values.replicaCount | default 1 }}
  selector:
    matchLabels:
      app: smarterp-admin
  template:
    metadata:
      labels:
        app: smarterp-admin
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
        - name: smarterp-admin
          image: "{{ .Values.global.imageRegistry }}/smarterp-admin:{{ .Values.imageTag | default .Values.global.imageTag }}"
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: DOTNET_ENVIRONMENT
              value: "Production"
            - name: ASPNETCORE_HTTP_PORTS
              value: "8080"
          envFrom:
            - secretRef:
                name: shared-secrets
            - secretRef:
                name: admin-secrets
