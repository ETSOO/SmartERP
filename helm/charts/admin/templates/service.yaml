apiVersion: v1
kind: Service
metadata:
  name: smarterp-admin
  namespace: smarterp
  labels:
    app: smarterp-admin
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: smarterp-admin
