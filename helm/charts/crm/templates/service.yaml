apiVersion: v1
kind: Service
metadata:
  name: smarterp-crm
  namespace: smarterp
  labels:
    app: smarterp-crm
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: smarterp-crm
