@model PlatformShared.Dto.ActionNoticeData

<!DOCTYPE html>

<html lang="zh-hant">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>@Model.Subject</title>
</head>
<body>
    <p>尊敬的使用者 <b>@Model.UserName</b>：</p>
    <p>在 @Model.FormatDateTime()，<b>@Model.Action</b>.</p>
    @if (!string.IsNullOrEmpty(Model.Detail))
    {
        <p>@Html.Raw(Model.Detail)</p>
    }
    <p>如果這不是您本人的操作，請第一時間聯絡我們進行幹預！</p>
    <p>IP位址：<b>@Model.IP</b></p>
    <hr style="width: 100%; background: none; border: none; border-top: 1px solid #666; height: 1px; margin: 6px 0px;" />
    <table border="0" width="100%">
        <tr>
            <td valign="middle"><a href="https://etsoo.com/" target="_blank"><img src="https://etsoo.com/images/etsoo.png" alt="ETSOO Logo" height="42" /></a></td>
            <td valign="middle" align="right"><small>億速思維 - 司友雲ERP<br />為中小企業技術賦能</small></td>
        </tr>
    </table>
</body>
</html>
